<div class="w-full flex justify-center">
  <div class="w-1/3">
    <%= simple_form_for [@plant, @plant_tag] do |f| %>
      <%#= f.association :tag %>
      <%= f.input :tag, collection: Tag.all, input_html: {multiple: true, class: "multiple-select"}, include_hidden: false %>
      <%= f.submit class: "bg-gray-800 text-white cursor-pointer rounded shadow px-4 py-2" %>
    <% end -%>

    <%#= form_for [@plant, @plant_tag] do |f| %>
      <%#= f.collection_select :tag_id, Tag.all, :id, :name %>
      <!-- <select name="" id="" multiple="multiple" class="multiple-select">
        <option value="1">Bug eating</option>
      </select> -->
    <%# end -%>
  </div>
</div>
